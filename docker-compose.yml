services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: discord-music-bot:latest
    container_name: discord-music-bot
    restart: unless-stopped
    env_file: .env   # put your secrets in a .env file in this folder
    ports:
      - "8889:8889"
